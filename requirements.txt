# Dependencias principales
pandas>=1.5.0
openpyxl>=3.0.0
Pillow>=9.0.0
qrcode>=7.0.0
reportlab>=4.0.0

# Dependencias para Windows (impresión y sistema)
pywin32>=300

# Dependencias para interfaz gráfica (tkinter viene con Python)
# No necesita instalación adicional

# Dependencias de PyInstaller para compilación
pyinstaller>=6.0.0

# Dependencias adicionales
six>=1.16.0